---
id: "5.5"
phase: 5
type: criar
depends_on: []
plan_file: plan-page-blocks-editorjs-v2.md
---

# ✅ 5.5 — Criar `<CtaBlock>`

## Contexto

Bloco de chamada para acao destacado com gradiente. Reusa visual exato do CTA final em `apps/web/modules/blog/ui/views/post-view.tsx:218-243` (rounded-3xl, `bg-gradient-to-br from-primary via-primary to-primary/80`, blobs blur, botao `<a>` rounded-full com `ArrowRight`).

## Detalhe tecnico

Criar arquivo novo. Server component.

**Imports:**
- `import { ArrowRight } from "lucide-react";`
- `import type { CtaBlockData } from "@workspace/types";`

**Props:**
```ts
interface CtaBlockProps {
  data: CtaBlockData;
}
```

**Estrutura visual** (copia direta de post-view.tsx:218-243, adaptando para dados dinamicos):

```tsx
return (
  <div className="my-16 md:my-20 max-w-5xl mx-auto px-4 sm:px-6">
    <section className="relative overflow-hidden rounded-3xl bg-gradient-to-br from-primary via-primary to-primary/80 px-6 py-12 md:px-12 md:py-20 text-center">
      <div
        aria-hidden
        className="pointer-events-none absolute -right-24 -top-24 h-72 w-72 rounded-full bg-primary-foreground/10 blur-3xl"
      />
      <div
        aria-hidden
        className="pointer-events-none absolute -left-32 -bottom-32 h-80 w-80 rounded-full bg-primary-foreground/10 blur-3xl"
      />
      <div className="relative max-w-2xl mx-auto">
        <h2 className="mb-4 text-3xl md:text-4xl font-bold text-primary-foreground tracking-tight">
          {data.heading}
        </h2>
        {data.description && (
          <p className="mb-8 text-base md:text-lg text-primary-foreground/90 leading-relaxed">
            {data.description}
          </p>
        )}
        <a
          href={data.buttonLink}
          className="inline-flex items-center gap-2 px-6 py-3 rounded-full bg-primary-foreground text-primary text-sm md:text-base font-semibold shadow-lg shadow-primary-foreground/20 transition-transform hover:scale-105"
        >
          {data.buttonText}
          <ArrowRight className="h-4 w-4" aria-hidden />
        </a>
      </div>
    </section>
  </div>
);
```

**Notas:**
- Wrapper externo `<div className="my-16 md:my-20 max-w-5xl mx-auto px-4 sm:px-6">` adiciona margem vertical e container — no `post-view.tsx` original a secao ja vinha dentro de `<main>` com layout pronto; aqui o CtaBlock pode aparecer em qualquer posicao do `OutputData` entao precisa do proprio espacamento.
- Botao SEMPRE renderiza (`buttonText` e `buttonLink` sao required em `CtaBlockData`).
- Descricao opcional.

## Arquivos tocados

- `apps/web/modules/pages/ui/components/blocks/cta-block.tsx` — criar

## Verificacao

- [ ] `pnpm --filter web typecheck` passa
- [ ] `npx biome check apps/web/modules/pages/ui/components/blocks/cta-block.tsx` passa
- [ ] Renderiza com `heading`, `description`, `buttonText`, `buttonLink` preenchidos
- [ ] Renderiza sem `description` (so heading + botao)
- [ ] Botao tem `hover:scale-105` funcionando no smoke test
- [ ] Sem `"use client"`

## Decisoes aplicadas

- **P10** (A): copia exata do CTA do post-view.tsx:218-243.
- **P11** (A): botao como `<a>` rounded-full com `ArrowRight`, sem `<Button>` shadcn.
- **P14** (A): server component puro.
