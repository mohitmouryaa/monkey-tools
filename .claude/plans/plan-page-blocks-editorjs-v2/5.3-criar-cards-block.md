---
id: "5.3"
phase: 5
type: criar
depends_on: []
plan_file: plan-page-blocks-editorjs-v2.md
---

# ✅ 5.3 — Criar `<CardsBlock>`

## Contexto

Grid responsivo de cards institucionais (titulo + descricao + icone opcional + link opcional). Componente novo, sem precedente direto no projeto — visual definido pelas decisoes P5/P6/P7.

## Detalhe tecnico

Criar arquivo novo. Server component.

**Imports:**
- `import { ArrowRight } from "lucide-react";`
- `import { DynamicIcon, type IconName } from "lucide-react/dynamic";`
- `import type { CardsBlockData } from "@workspace/types";`

**Props:**
```ts
interface CardsBlockProps {
  data: CardsBlockData;
}
```

**Estrutura visual:**

```tsx
const cards = data.cards ?? [];

return (
  <section className="py-16 md:py-20">
    <div className="max-w-6xl mx-auto px-4 sm:px-6">
      {(data.title || data.subtitle) && (
        <div className="text-center mb-10 md:mb-14 max-w-3xl mx-auto">
          {data.title && (
            <h2 className="text-3xl md:text-4xl font-bold text-foreground mb-4">{data.title}</h2>
          )}
          {data.subtitle && (
            <p className="text-base md:text-lg text-muted-foreground">{data.subtitle}</p>
          )}
        </div>
      )}

      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {cards.map((card, index) => (
          <div
            key={`${card.title}-${index}`}
            className="rounded-xl border bg-card p-6 transition-shadow hover:shadow-md"
          >
            {card.iconName && (
              <div className="mb-4 flex size-10 items-center justify-center rounded-lg bg-primary/10 text-primary">
                <DynamicIcon name={card.iconName as IconName} className="size-5" />
              </div>
            )}
            <h3 className="text-lg font-semibold text-foreground mb-2">{card.title}</h3>
            <p className="text-sm text-muted-foreground leading-relaxed">{card.description}</p>
            {card.linkLabel && card.linkHref && (
              <a
                href={card.linkHref}
                className="mt-4 inline-flex items-center gap-1 text-sm font-medium text-primary hover:underline"
              >
                {card.linkLabel}
                <ArrowRight className="h-4 w-4" aria-hidden />
              </a>
            )}
          </div>
        ))}
      </div>
    </div>
  </section>
);
```

**Notas:**
- Sem fallback de icone aqui — se `iconName` for invalido, `DynamicIcon` renderiza nada por default. Isso eh OK para cards (icone eh decorativo/opcional). Diferente de steps que tem fallback `Upload`.
- Sem `<Button>` shadcn no link — `<a>` puro com seta `ArrowRight`. Decisao P7.
- Container `max-w-6xl` (mais largo que outros) para acomodar grid 3-col confortavel.
- Sem hover de scale — somente shadow (decoracao discreta).

## Arquivos tocados

- `apps/web/modules/pages/ui/components/blocks/cards-block.tsx` — criar

## Verificacao

- [ ] `pnpm --filter web typecheck` passa
- [ ] `npx biome check apps/web/modules/pages/ui/components/blocks/cards-block.tsx` passa
- [ ] Renderiza com 3 cards (1 com icone, 1 sem icone, 1 com link)
- [ ] Renderiza com 6 cards (grid 3-col em desktop)
- [ ] Renderiza com 1 card (grid 1-col em mobile)
- [ ] Card sem `linkLabel` nao mostra `<a>`
- [ ] Card sem `iconName` nao mostra o quadrado de icone
- [ ] Sem `"use client"`

## Decisoes aplicadas

- **P5** (A): grid `grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`.
- **P6** (A): card `rounded-xl border bg-card p-6`. Icone em quadrado `size-10 rounded-lg bg-primary/10 text-primary`.
- **P7** (A): link como `<a>` com `ArrowRight`, sem `<Button>` shadcn.
- **P14** (A): server component puro.
