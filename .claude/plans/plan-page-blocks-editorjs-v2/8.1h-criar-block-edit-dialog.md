---
id: 8.1h
phase: 8
type: criar
depends_on: [8.1a, 8.1b, 8.1c, 8.1d, 8.1e, 8.1f, 8.1g]
plan_file: plan-page-blocks-editorjs-v2.md
---

✅ # 8.1h — Criar `BlockEditDialog` (dispatcher)

## Contexto

`<Dialog>` controlado que renderiza condicionalmente um dos 6 form-modals (8.1b–g) baseado em `editingBlock.type`. O `EditorJSField` (8.1i) controla `editingBlock` via `useState` e passa esse dispatcher como filho. Cada plugin custom tem um callback `openEditor` que seta `editingBlock`; ao salvar, o callback `onSave` original do plugin é invocado e o dialog fecha.

## Detalhe técnico

Criar `apps/web/modules/dashboard/ui/components/editor-js-field/block-edit-dialog.tsx` (`"use client"`).

```tsx
"use client";

import { Dialog } from "@workspace/ui/components/dialog";
import { CardsFormModal } from "./cards-form-modal";
import { CtaFormModal } from "./cta-form-modal";
import { FaqFormModal } from "./faq-form-modal";
import { HeroFormModal } from "./hero-form-modal";
import { RawHtmlFormModal } from "./raw-html-form-modal";
import { StepsFormModal } from "./steps-form-modal";
import type { EditingBlock } from "./types";

interface BlockEditDialogProps {
  editing: EditingBlock | null;
  onClose: () => void;
}

export const BlockEditDialog = ({ editing, onClose }: BlockEditDialogProps) => {
  const open = editing !== null;

  // Adapter: cada modal recebe `onSave` específico do tipo do bloco. O `editing.onSave`
  // já é tipado pelo discriminante em `EditingBlock`. Em runtime, o switch garante a
  // correlação certa; em compile-time, fazemos o cast pelo type assertion controlado.
  const handleSave = (next: unknown) => {
    if (!editing) return;
    // O `editing.onSave` espera o shape correspondente ao `editing.type`.
    // O modal renderizado abaixo é escolhido pelo mesmo discriminante, então o `next`
    // que ele entrega bate com o esperado por `editing.onSave`.
    // biome-ignore lint/suspicious/noExplicitAny: dispatch tipado pelo discriminante `editing.type`
    (editing.onSave as (n: any) => void)(next);
    onClose();
  };

  return (
    <Dialog open={open} onOpenChange={(next) => (next ? null : onClose())}>
      {editing?.type === "hero" ? (
        <HeroFormModal open={open} initialData={editing.data} onCancel={onClose} onSave={handleSave} />
      ) : null}
      {editing?.type === "steps" ? (
        <StepsFormModal open={open} initialData={editing.data} onCancel={onClose} onSave={handleSave} />
      ) : null}
      {editing?.type === "cards" ? (
        <CardsFormModal open={open} initialData={editing.data} onCancel={onClose} onSave={handleSave} />
      ) : null}
      {editing?.type === "faq" ? (
        <FaqFormModal open={open} initialData={editing.data} onCancel={onClose} onSave={handleSave} />
      ) : null}
      {editing?.type === "cta" ? (
        <CtaFormModal open={open} initialData={editing.data} onCancel={onClose} onSave={handleSave} />
      ) : null}
      {editing?.type === "raw-html" ? (
        <RawHtmlFormModal open={open} initialData={editing.data} onCancel={onClose} onSave={handleSave} />
      ) : null}
    </Dialog>
  );
};
```

**Por quê o cast `any`:**
- `EditingBlock<T>` é genérico em `T`, mas o estado `editingBlock` no `EditorJSField` é `EditingBlock<EditableBlockType>` (união). TypeScript não estreita `T` em funções intermediárias — o `switch (editing.type)` estreita `editing.data` mas não `editing.onSave` (que captura o `T` da chamada original). O cast é local, justificado e seguro porque o switch garante o casamento em runtime. Comentar com `biome-ignore` é o padrão já usado no projeto (ver `editor-js/index.tsx` linha 49 e `custom-page-form.tsx:39`).

**Por quê o `<Dialog open onOpenChange>`:** segue o padrão do shadcn (e do `spike-editorjs-view.tsx:121`) — `open` controlado, `onOpenChange` chama `onClose` quando o usuário clica fora ou tecla Escape.

## Arquivos tocados

- `apps/web/modules/dashboard/ui/components/editor-js-field/block-edit-dialog.tsx` — novo.

## Verificação

- [ ] Typecheck e lint passam.
- [ ] Importa exatamente os 6 form-modals criados em 8.1b–g.
- [ ] `<Dialog>` fecha ao clicar fora ou tecla Escape (delegado ao shadcn — testar manualmente na 8.1i).

## Decisões aplicadas

- **P1** (B): este componente é o dispatcher exigido pela escolha "6 form-modals + 1 dispatcher".
- **P2** (A): não tem estado próprio — recebe `editing` e `onClose` do `EditorJSField`.
