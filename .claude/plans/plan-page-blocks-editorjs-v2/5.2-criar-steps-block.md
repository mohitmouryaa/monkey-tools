---
id: "5.2"
phase: 5
type: criar
depends_on: []
plan_file: plan-page-blocks-editorjs-v2.md
---

# ✅ 5.2 — Criar `<StepsBlock>`

## Contexto

Componente server-side que renderiza uma sequencia de passos (icone + titulo + descricao) num grid horizontal com linha conectora. Reusa o visual de `apps/web/modules/hero/ui/components/how-it-works.tsx` mas **sem** o bloco final de trust badges (`Shield/Zap/Lock/FileText`). Sem `"use client"`.

## Detalhe tecnico

Criar arquivo novo. Server component.

**Imports:**
- `import { Upload } from "lucide-react";`
- `import { DynamicIcon, type IconName } from "lucide-react/dynamic";`
- `import type { StepsBlockData } from "@workspace/types";`

**Props:**
```ts
interface StepsBlockProps {
  data: StepsBlockData;
}
```

**Estrutura visual** (copia de how-it-works.tsx:60-84, removendo trust badges):

```tsx
const steps = data.steps ?? [];

return (
  <section className="bg-muted/70 border-t border-border py-16 md:py-20">
    <div className="max-w-5xl mx-auto px-4 sm:px-6 flex flex-col items-center">
      {(data.title || data.subtitle) && (
        <div className="text-center mb-12 md:mb-16 max-w-3xl mx-auto">
          {data.title && (
            <h2 className="text-3xl md:text-4xl lg:text-5xl font-bold text-foreground mb-5">{data.title}</h2>
          )}
          {data.subtitle && (
            <p className="text-base md:text-lg text-muted-foreground">{data.subtitle}</p>
          )}
        </div>
      )}

      <div className="relative grid grid-cols-1 md:grid-cols-3 gap-14 md:gap-10 w-full justify-items-center">
        {steps.length === 3 && (
          <div className="hidden md:block absolute top-12 left-[16.66%] right-[16.66%] h-px bg-border" aria-hidden />
        )}
        {steps.map((step, index) => (
          <div key={`${step.title}-${index}`} className="relative flex flex-col items-center text-center max-w-xs">
            <div className="w-24 h-24 rounded-2xl bg-primary flex items-center justify-center mb-6 shadow-lg shadow-primary/20 relative z-10">
              <DynamicIcon
                name={step.iconName as IconName}
                className="w-10 h-10 text-primary-foreground"
                fallback={() => <Upload className="w-10 h-10 text-primary-foreground" />}
              />
            </div>
            <h3 className="text-lg font-semibold text-foreground mb-2">
              {index + 1}. {step.title}
            </h3>
            <p className="text-sm text-muted-foreground leading-relaxed">{step.description}</p>
          </div>
        ))}
      </div>
    </div>
  </section>
);
```

**Notas:**
- Linha conectora so aparece se houver exatamente 3 passos (calibrada para o grid 3-col). Para 2 ou 4+ passos, omitida — evita visual quebrado. Layout do grid `md:grid-cols-3` funciona para qualquer N pois flex-justify-items-center e wrap natural.
- Numeracao automatica `{index + 1}.` antes do titulo (mesmo padrao do how-it-works).
- Fallback de icone: `Upload` (mesmo do how-it-works:75).
- `DynamicIcon` eh client-only? Nao — eh um componente que carrega dinamicamente o icone via `dynamic()` interno. Funciona em RSC desde que importado em server component (lucide-react/dynamic suporta SSR). Se der erro de hidratacao no smoke test 5.7, trocar para `"use client"`.

## Arquivos tocados

- `apps/web/modules/pages/ui/components/blocks/steps-block.tsx` — criar

## Verificacao

- [ ] `pnpm --filter web typecheck` passa
- [ ] `npx biome check apps/web/modules/pages/ui/components/blocks/steps-block.tsx` passa
- [ ] Renderiza com 3 passos (com linha conectora)
- [ ] Renderiza com 2 passos (sem linha)
- [ ] Renderiza com 4 passos (sem linha, grid wrap)
- [ ] `iconName: "Upload"`, `iconName: "Settings"`, `iconName: "Download"` resolvem
- [ ] `iconName: "FooBar"` (invalido) cai no fallback `Upload`
- [ ] Sem `"use client"`

## Decisoes aplicadas

- **P3** (A): copia do how-it-works.tsx sem trust badges.
- **P4** (A): fallback `Upload` quando DynamicIcon nao resolve.
- **P14** (A): server component puro.
