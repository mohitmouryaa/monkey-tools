---
id: 4.17
phase: 4
type: criar
depends_on: [4.1, 4.13]
plan_file: plan-cms-blog-v2.md
---

# ✅ 4.17 — Criar `app/(main)/blog/[slug]/page.tsx` (post detail)

## Contexto
Página pública de post individual. Server component que faz fetch via
`caller.posts.getBySlug` envolto em `unstable_cache` com tags
`["blog", \`blog:${slug}\`]` e `revalidate: 60` (P8 = A). Implementa
`generateMetadata` aplicando os fallbacks `title→post.title`,
`description→post.excerpt`, `ogImage→post.coverImage` (já indicados no
plano). Renderiza o conteúdo via 4.13 (`PostContentRenderer`) e mostra
"tools relacionadas" no rodapé via 4.1 (`PostCard`) — conforme critério
de conclusão da fase.

## Detalhe técnico
Criar `apps/web/app/(main)/blog/[slug]/page.tsx`.

```tsx
import type { Metadata } from "next";
import { notFound } from "next/navigation";
import Image from "next/image";
import { unstable_cache } from "next/cache";
import { caller } from "@/trpc/server";
import { PostContentRenderer } from "@/modules/blog/ui/components/post-content-renderer";

interface BlogSlugPageProps {
  params: Promise<{ slug: string }>;
}

const fetchPostBySlug = (slug: string) =>
  unstable_cache(
    async () => {
      try {
        return await caller.posts.getBySlug({ slug });
      } catch {
        return null;
      }
    },
    ["blog-post-v1", slug],
    { revalidate: 60, tags: ["blog", `blog:${slug}`] },
  )();

export async function generateMetadata({ params }: BlogSlugPageProps): Promise<Metadata> {
  const { slug } = await params;
  const post = await fetchPostBySlug(slug);

  if (!post) {
    return {
      title: "Post não encontrado",
      description: "O artigo solicitado não foi encontrado.",
    };
  }

  // Fallbacks conforme critério da Fase 4
  const title = post.seo?.title ?? post.title;
  const description = post.seo?.description ?? post.excerpt;
  const ogImage = post.seo?.ogImage ?? post.coverImage;

  return {
    title,
    description,
    openGraph: {
      title,
      description,
      type: "article",
      images: ogImage ? [{ url: ogImage }] : undefined,
      publishedTime: post.publishedAt ? new Date(post.publishedAt).toISOString() : undefined,
    },
    twitter: {
      card: "summary_large_image",
      title,
      description,
      images: ogImage ? [ogImage] : undefined,
    },
  };
}

export default async function BlogSlugPage({ params }: BlogSlugPageProps) {
  const { slug } = await params;
  const post = await fetchPostBySlug(slug);

  if (!post) notFound();

  return (
    <article className="container px-4 py-12 mx-auto max-w-4xl">
      <header className="mb-10">
        <h1 className="text-3xl font-bold tracking-tight text-foreground md:text-4xl">{post.title}</h1>
        {post.excerpt && (
          <p className="mt-3 text-lg text-muted-foreground">{post.excerpt}</p>
        )}
      </header>

      {post.coverImage && (
        <div className="relative w-full mb-10 overflow-hidden rounded-2xl aspect-[16/9]">
          <Image
            src={post.coverImage}
            alt={post.title}
            fill
            sizes="(min-width: 1024px) 800px, 100vw"
            className="object-cover"
            priority
          />
        </div>
      )}

      <PostContentRenderer
        content={post.content}
        tools={(post.tools ?? []) as Array<{ _id: string; title: string; link: string }>}
      />

      {post.tools && post.tools.length > 0 && (
        <footer className="pt-10 mt-12 border-t border-border">
          <h2 className="mb-4 text-xl font-semibold text-foreground">Ferramentas relacionadas</h2>
          <ul className="space-y-2">
            {(post.tools as Array<{ _id: string; title: string }>).map((tool) => (
              <li key={tool._id} className="text-muted-foreground">
                {tool.title}
              </li>
            ))}
          </ul>
        </footer>
      )}
    </article>
  );
}
```

Notas:
- `fetchPostBySlug` cria a função cacheada **por slug** dinamicamente —
  isso é necessário para a key do `unstable_cache` distinguir por slug.
  O `try/catch` engloba o `TRPCError NOT_FOUND` para evitar throw em
  build.
- `tools` populadas vêm do router (`postsRouter.getBySlug`); aqui só
  passamos pra frente. A asserção de tipo `as Array<{...}>` é segura
  porque o router popula com `.populate("tools")`.
- "Ferramentas relacionadas" no rodapé fica como lista simples (nome).
  Não usar `PostCard` aqui — `PostCard` é para *posts*, não tools.
  Versão mais rica (links pra tool) pode vir junto da Fase 5 quando o
  cross-link estiver pronto.
- `priority` no cover image (acima da dobra) por LCP.

## Arquivos tocados
- `apps/web/app/(main)/blog/[slug]/page.tsx` — criar.

## Verificação
- [ ] `pnpm --filter web typecheck` passa.
- [ ] `/blog/<slug-inexistente>` retorna 404 (via `notFound()`).
- [ ] `/blog/<slug>` mostra título, cover, conteúdo renderizado.
- [ ] Post com TODOS os 9 blocos (Header, Paragraph, List, Quote, Embed,
      Table, Checklist, Image, ToolEmbed) renderiza sem erro.
- [ ] OG tags presentes no HTML (inspecionar via view-source).
- [ ] Tool Embed dentro do conteúdo renderiza o componente real da tool.
- [ ] Cache: editar e chamar `revalidateTag(\`blog:${slug}\`)` reflete
      imediato; sem dispatch, reflete em até 60s.

## Decisões aplicadas
- **P1** (A): renderer custom via 4.13.
- **P2** (A): server component.
- **P8** (A): `revalidate: 60` também aqui.
