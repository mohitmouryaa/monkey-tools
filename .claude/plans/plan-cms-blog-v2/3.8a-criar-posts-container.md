---
id: 3.8a
phase: 3
type: criar
depends_on: [3.2a]
plan_file: plan-cms-blog-v2.md
---

# ✅ 3.8a — Criar `posts-container.tsx`

## Contexto
Wrapper de página que combina header + search + children + pagination. Análogo de `tools-container.tsx` mas usando os entity-components dedicados de 3.2a.

## Detalhe técnico
Criar `apps/web/modules/dashboard/ui/components/posts-container.tsx`:

```tsx
import { PostsContainer as Layout } from "@/modules/dashboard/posts/ui/posts-entity-components";
import { PostsHeader } from "@/modules/dashboard/ui/components/posts-header";
import { PostsSearch } from "@/modules/dashboard/ui/components/posts-search";
import { PostsPagination } from "@/modules/dashboard/ui/components/posts-pagination";

export const PostsContainer = ({ children }: { children: React.ReactNode }) => {
  return (
    <Layout header={<PostsHeader />} search={<PostsSearch />} pagination={<PostsPagination />}>
      {children}
    </Layout>
  );
};
```

(Se `PostsContainer` em 3.2a foi exportado com nome conflitante, importar com alias `PostsContainer as Layout`.)

## Arquivos tocados
- `apps/web/modules/dashboard/ui/components/posts-container.tsx` — criar.

## Verificação
- [ ] `pnpm --filter web typecheck` passa.
