---
id: 4.16
phase: 4
type: criar
depends_on: [4.1, 4.2, 4.3, 4.14, 4.15]
plan_file: plan-cms-blog-v2.md
---

# ✅ 4.16 — Criar `app/(main)/blog/page.tsx` (listagem)

## Contexto
Página pública de listagem do blog. Server component que faz fetch via
`caller.posts.list` + `caller.posts.getFeatured`, envolto em
`unstable_cache` com tag `["blog"]` e `revalidate: 60` (P8 = A — TTL
fallback de 60s).

## Detalhe técnico
Criar `apps/web/app/(main)/blog/page.tsx`.

```tsx
import type { Metadata } from "next";
import { unstable_cache } from "next/cache";
import { caller } from "@/trpc/server";
import { PostCard } from "@/modules/blog/ui/components/post-card";
import { PostFilterBar } from "@/modules/blog/ui/components/post-filter-bar";
import { FeaturedPostsSection } from "@/modules/blog/ui/components/featured-posts-section";
import { BlogPagination } from "@/modules/blog/ui/components/blog-pagination";
import { PAGINATION } from "@/modules/common/constants";
import { PostStatus } from "@workspace/types";

export const metadata: Metadata = {
  title: "Blog - Monkey Tools",
  description: "Aprenda a usar nossas ferramentas com tutoriais, dicas e novidades.",
};

interface BlogPageProps {
  searchParams: Promise<{
    page?: string;
    q?: string;
    tool?: string;
  }>;
}

const fetchBlogList = unstable_cache(
  async (page: number, pageSize: number, q: string, toolId: string | undefined) => {
    const [list, featured] = await Promise.all([
      caller.posts.list({
        page,
        pageSize,
        search: q,
        toolId,
        status: PostStatus.PUBLISHED,
      }),
      caller.posts.getFeatured({ limit: 3 }),
    ]);
    return { list, featured };
  },
  ["blog-list-v1"],
  { revalidate: 60, tags: ["blog"] },
);

export default async function BlogPage({ searchParams }: BlogPageProps) {
  const sp = await searchParams;
  const page = Number(sp.page) > 0 ? Number(sp.page) : PAGINATION.DEFAULT_PAGE;
  const pageSize = PAGINATION.DEFAULT_PAGE_SIZE;
  const q = sp.q ?? "";
  const tool = sp.tool || undefined;

  const { list, featured } = await fetchBlogList(page, pageSize, q, tool);

  // Em listagem regular, NÃO duplicar posts featured no topo se eles
  // já apareceriam abaixo. Mantemos simples: featured só aparece na 1a
  // página e sem filtros.
  const showFeatured = page === 1 && !q && !tool;

  return (
    <div className="container px-4 py-12 mx-auto max-w-6xl">
      <header className="mb-10">
        <h1 className="text-4xl font-bold tracking-tight text-foreground">Blog</h1>
        <p className="mt-2 text-muted-foreground">
          Aprenda a usar nossas ferramentas com tutoriais, dicas e novidades.
        </p>
      </header>

      <div className="mb-8">
        <PostFilterBar />
      </div>

      {showFeatured && <FeaturedPostsSection posts={featured} />}

      {list.items.length === 0 ? (
        <div className="py-16 text-center text-muted-foreground">
          Nenhum artigo encontrado.
        </div>
      ) : (
        <div className="grid grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-3">
          {list.items.map((post) => (
            <PostCard key={post._id} post={post} />
          ))}
        </div>
      )}

      <BlogPagination page={list.page} totalPages={list.totalPages} />
    </div>
  );
}
```

Notas:
- `unstable_cache` é usado por consistência com o resto do projeto
  (Stack & Convenções do plano). Migração para Cache Components fica
  fora de escopo (decisão registrada no plano).
- `revalidate: 60` é o TTL fallback (Premissa 7) para posts agendados.
- `searchParams` em Next 16 é `Promise<...>` — `await` necessário.
- `caller.posts.list` retorna apenas posts publicados quando chamado sem
  sessão (server-side aqui sem ctx.session). Mesmo assim passamos
  `status: PostStatus.PUBLISHED` por explicitude — o router aceita o
  filtro extra sem problema.
- A página NÃO importa o `blogParams` server (não é necessário) — usa
  `searchParams` direto e parseia inline; o nuqs server-side só é
  necessário em layouts especiais. Manter simples.

## Arquivos tocados
- `apps/web/app/(main)/blog/page.tsx` — criar.

## Verificação
- [ ] `pnpm --filter web typecheck` passa.
- [ ] Acessar `/blog` retorna HTML com lista de posts publicados.
- [ ] `?q=foo` filtra por busca.
- [ ] `?tool=<id>` filtra por tool.
- [ ] `?page=2` paginar funciona.
- [ ] Featured aparece só na primeira página sem filtros.
- [ ] Cache: editar um post + chamar `revalidateTag("blog")` reflete em
      no máximo 60s (TTL) ou imediatamente após o tag dispatch.

## Decisões aplicadas
- **P2** (A): server component.
- **P6** (A): filtros como client (4.2); página server.
- **P7** (A): paginação prev/next.
- **P8** (A): `revalidate: 60`.
