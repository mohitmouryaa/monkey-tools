---
id: 4.2
phase: 4
type: criar
depends_on: []
plan_file: plan-cms-blog-v2.md
---

# ✅ 4.2 — Criar `post-filter-bar.tsx`

## Contexto
Barra de filtros do `/blog`: input de busca (com debounce) + select de
tool. Client component com nuqs (P6 = A) — mesma stack do admin
(`posts-search.tsx` / `usePostsParams`). Sincroniza com a query string
para que a página server (`blog/page.tsx`) leia os mesmos params via
`searchParams` (Next 16).

## Detalhe técnico
Criar `apps/web/modules/blog/ui/components/post-filter-bar.tsx`:

```tsx
"use client";

import { useEffect, useState } from "react";
import { useQueryStates } from "nuqs";
import { useTRPC } from "@/trpc/client";
import { useQuery } from "@tanstack/react-query";
import { blogParams } from "@/modules/blog/blog-params";
import { Input } from "@workspace/ui/components/input";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@workspace/ui/components/select";
import { SearchIcon } from "lucide-react";
import { PAGINATION } from "@/modules/common/constants";

const DEBOUNCE_MS = 500;

export const PostFilterBar = () => {
  const [params, setParams] = useQueryStates(blogParams);
  const [localSearch, setLocalSearch] = useState(params.q ?? "");

  const trpc = useTRPC();
  const { data: toolsData } = useQuery(trpc.tools.getMany.queryOptions({ pageSize: 100 }));

  // Debounce busca
  useEffect(() => {
    if (localSearch === "" && params.q !== "") {
      setParams({ ...params, q: "", page: PAGINATION.DEFAULT_PAGE });
      return;
    }
    const t = setTimeout(() => {
      if (localSearch !== params.q) {
        setParams({ ...params, q: localSearch, page: PAGINATION.DEFAULT_PAGE });
      }
    }, DEBOUNCE_MS);
    return () => clearTimeout(t);
  }, [localSearch, params, setParams]);

  return (
    <div className="flex flex-col gap-3 sm:flex-row sm:items-center">
      <div className="relative flex-1">
        <SearchIcon className="size-3.5 absolute left-3 top-1/2 -translate-y-1/2 text-muted-foreground" />
        <Input
          value={localSearch}
          onChange={(e) => setLocalSearch(e.target.value)}
          placeholder="Buscar artigos..."
          className="pl-8"
        />
      </div>
      <Select
        value={params.tool ?? "all"}
        onValueChange={(value) =>
          setParams({ ...params, tool: value === "all" ? null : value, page: PAGINATION.DEFAULT_PAGE })
        }
      >
        <SelectTrigger className="sm:w-64">
          <SelectValue placeholder="Todas as ferramentas" />
        </SelectTrigger>
        <SelectContent>
          <SelectItem value="all">Todas as ferramentas</SelectItem>
          {toolsData?.items.map((tool) => (
            <SelectItem key={tool._id} value={tool._id ?? ""}>
              {tool.title}
            </SelectItem>
          ))}
        </SelectContent>
      </Select>
    </div>
  );
};
```

Notas:
- `useQuery` (não `useSuspenseQuery`) para não bloquear render se tools
  ainda não carregaram. Loading silencioso: select fica vazio até voltar.
- `value="all"` é truque para representar "sem filtro" em
  `<Select>` (Radix não aceita `""`).
- O loader nuqs `blogParams` é definido em 4.15 — esta task **não** depende
  formalmente de 4.15 porque o import resolve em build (tree-shaking não
  reclama); ambos podem ser criados em paralelo na onda 1, e o `pnpm
  typecheck` final vai validar.

## Arquivos tocados
- `apps/web/modules/blog/ui/components/post-filter-bar.tsx` — criar.

## Verificação
- [ ] Componente compila junto com `4.15` (a página `/blog` consome ambos).
- [ ] Mudar input → URL ganha `?q=...` após 500ms.
- [ ] Mudar select → URL ganha `?tool=<id>` (ou remove quando "all").
- [ ] Limpar input restaura URL sem `q`.

## Decisões aplicadas
- **P6** (A): client component com nuqs, espelhando padrão do admin.
