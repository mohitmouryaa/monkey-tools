---
id: 3.1a
phase: 3
type: criar
depends_on: []
plan_file: plan-cms-blog-v2.md
---

# ✅ 3.1a — Criar `posts-params.tsx` (nuqs) dedicado

## Contexto
Listagem de posts em `/dashboard/posts` precisa de query params persistidos na URL (page, pageSize, search, status). Por regra do usuário, NÃO reusar `tool-params.tsx` — criar análogo dedicado.

## Detalhe técnico
Criar `apps/web/modules/dashboard/posts/posts-params.tsx`:

```ts
import { parseAsInteger, parseAsString, parseAsStringEnum } from "nuqs/server";
import { PostStatus } from "@workspace/types";
import { PAGINATION } from "@/modules/common/constants";

export const postsParams = {
  page: parseAsInteger.withDefault(PAGINATION.DEFAULT_PAGE).withOptions({ clearOnDefault: true }),
  pageSize: parseAsInteger.withDefault(PAGINATION.DEFAULT_PAGE_SIZE).withOptions({ clearOnDefault: true }),
  search: parseAsString.withDefault("").withOptions({ clearOnDefault: true }),
  status: parseAsStringEnum<PostStatus>(Object.values(PostStatus)).withOptions({ clearOnDefault: true }),
};
```

`status` é opcional (sem default) — quando não vem na URL, lista todos os status (admin).

## Arquivos tocados
- `apps/web/modules/dashboard/posts/posts-params.tsx` — criar.

## Verificação
- [ ] `import { postsParams } from "@/modules/dashboard/posts/posts-params"` resolve.
- [ ] `pnpm --filter web typecheck` passa.

## Decisões aplicadas
- Regra de não-reuso do usuário: cria arquivo dedicado em vez de tocar `tool-params.tsx`.
