---
id: 3.4a
phase: 3
type: criar
depends_on: [3.0a]
plan_file: plan-cms-blog-v2.md
---

# ✅ 3.4a — Adicionar `<Calendar>` shadcn em `packages/ui`

## Contexto
DatePicker para `publishedAt` precisa do componente `<Calendar>` shadcn (wrapper de `react-day-picker`).

## Detalhe técnico
Criar `packages/ui/src/components/calendar.tsx` seguindo o template oficial do shadcn:

- API `react-day-picker` v9. (`DayPicker`, `useDayPicker`, etc.)
- Estilização Tailwind via `cn` de `@workspace/ui/lib/utils`.
- Exportar `Calendar` (default config) e tipos `CalendarProps = React.ComponentProps<typeof DayPicker>`.

Conteúdo de referência: https://ui.shadcn.com/docs/components/calendar

Pontos não-negociáveis para casar com o restante do design system:
- Usar `Button` de `@workspace/ui/components/button` se o template usar `buttonVariants`.
- `chevronIcons` ou equivalente (ChevronLeftIcon/ChevronRightIcon) de `lucide-react`.
- Classes alinhadas ao tema (`bg-background`, `text-foreground`, etc.) — não inventar cores.

Testar isoladamente: importar em um componente de teste, montar e ver se o calendário renderiza com tema do projeto.

## Arquivos tocados
- `packages/ui/src/components/calendar.tsx` — criar.

## Verificação
- [ ] `pnpm --filter @workspace/ui build` passa (ou typecheck, se for o que o package faz).
- [ ] `import { Calendar } from "@workspace/ui/components/calendar"` resolve em `apps/web`.

## Decisões aplicadas
- **P2 (B)**: adicionar `<Calendar>` shadcn em `packages/ui`.
