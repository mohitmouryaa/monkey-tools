---
id: 3.11
phase: 3
type: modificar
depends_on: []
plan_file: plan-cms-blog-v2.md
---

# ✅ 3.11 — Adicionar entrada "Posts" no sidebar

## Contexto
Plano original da Fase 3 pede entrada nova no `app-sidebar.tsx`, ícone `Newspaper`, rota `/dashboard/posts`.

## Detalhe técnico
Em `apps/web/modules/dashboard/ui/components/app-sidebar.tsx`:

1. Adicionar `Newspaper` no import de `lucide-react`:
   ```ts
   import { LayoutDashboard, Search, Settings, Wrench, Tag, FileText, Code, Newspaper } from "lucide-react";
   ```
2. Adicionar item no `data.navMain`, posicionado após "Pages" (entre Pages e Scripts) ou outra ordem — escolher a ordem alinhada ao restante:
   ```ts
   {
     title: "Posts",
     url: "/dashboard/posts",
     icon: Newspaper,
   },
   ```

Posicionamento sugerido: após `FileText` ("Pages") e antes de `Code` ("Scripts") — agrupa conteúdo editorial.

## Arquivos tocados
- `apps/web/modules/dashboard/ui/components/app-sidebar.tsx` — adicionar import e item de nav.

## Verificação
- [ ] `pnpm --filter web typecheck` passa.
- [ ] Sidebar mostra "Posts" com ícone Newspaper.
- [ ] Click navega para `/dashboard/posts`.
