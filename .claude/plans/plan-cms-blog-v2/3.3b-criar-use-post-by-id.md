---
id: 3.3b
phase: 3
type: criar
depends_on: []
plan_file: plan-cms-blog-v2.md
---

# ✅ 3.3b — Criar hook `use-post-by-id`

## Contexto
Edit view consome `posts.getById` (admin, sem filtro de status).

## Detalhe técnico
Criar `apps/web/modules/dashboard/hooks/use-post-by-id.tsx`:

```tsx
"use client";
import { useTRPC } from "@/trpc/client";
import { useSuspenseQuery } from "@tanstack/react-query";

export const usePostById = (id: string) => {
  const trpc = useTRPC();
  return useSuspenseQuery(trpc.posts.getById.queryOptions({ id }));
};
```

Usar `useSuspenseQuery` (alinhado com o padrão dos outros hooks do dashboard, ex: `useSuspenseTool`).

## Arquivos tocados
- `apps/web/modules/dashboard/hooks/use-post-by-id.tsx` — criar.

## Verificação
- [ ] `pnpm --filter web typecheck` passa.
