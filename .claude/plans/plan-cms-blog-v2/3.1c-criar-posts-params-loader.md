---
id: 3.1c
phase: 3
type: criar
depends_on: [3.1a]
plan_file: plan-cms-blog-v2.md
---

# ✅ 3.1c — Criar `posts-params-loader` server-side

## Contexto
Server components precisam parsear `searchParams` para passar ao prefetch. Análogo de `params-loader.tsx`, dedicado a posts.

## Detalhe técnico
Criar `apps/web/modules/dashboard/posts/server/params-loader.tsx`:

```ts
import { createLoader } from "nuqs/server";
import { postsParams } from "@/modules/dashboard/posts/posts-params";

export const postsParamsLoader = createLoader(postsParams);
```

NÃO modificar `apps/web/modules/dashboard/server/params-loader.tsx` (compartilhado entre tools/categories).

## Arquivos tocados
- `apps/web/modules/dashboard/posts/server/params-loader.tsx` — criar.

## Verificação
- [ ] `import { postsParamsLoader } from "@/modules/dashboard/posts/server/params-loader"` resolve em server components.
- [ ] `pnpm --filter web typecheck` passa.

## Decisões aplicadas
- Regra de não-reuso: criou loader dedicado.
