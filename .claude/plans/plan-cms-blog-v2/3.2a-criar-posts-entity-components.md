---
id: 3.2a
phase: 3
type: criar
depends_on: []
plan_file: plan-cms-blog-v2.md
---

# ✅ 3.2a — Criar `posts-entity-components` (análogos dedicados)

## Contexto
Substitutos dedicados dos `EntityHeader/EntityContainer/EntitySearch/EntityPagination/EntityList/EntityItem/EmptyView` de `modules/common`. Mesmo comportamento visual, código próprio (regra de não-reuso).

## Detalhe técnico
Criar `apps/web/modules/dashboard/posts/ui/posts-entity-components.tsx` exportando:

- `PostsHeader` — title, description, botão "New post" (suporte a `onNew` ou `newButtonHref`).
- `PostsContainer` — wrapper de página (header + search slot + children + pagination slot).
- `PostsSearchInput` — input com ícone de busca (controlled `value` + `onChange`).
- `PostsPaginationBar` — "Page X of Y" + Previous/Next.
- `PostsList<T>` — lista flex-col com `renderItem` + `getKey` + `emptyView`.
- `PostsListItem` — Card clicável com title/subtitle/image/actions/onRemove (DropdownMenu Delete).
- `PostsEmpty` — Empty + "No posts" + CTA "Add post".

Não copiar literalmente do `entity-components.tsx` — escrever do zero replicando aparência (mesmas classes Tailwind), mas com nomes locais e tipagens próprias.

Exemplo de assinatura mínima:

```tsx
"use client";
// imports de @workspace/ui, lucide-react, next/link, etc.

export const PostsContainer = ({ header, search, pagination, children }: { header?: React.ReactNode; search?: React.ReactNode; pagination?: React.ReactNode; children: React.ReactNode }) => { /* ... */ };

export const PostsHeader = ({ title, description, newButtonLabel, newButtonHref, onNew, disabled, isCreating }: { /* ... */ }) => { /* ... */ };

export const PostsSearchInput = ({ value, onChange, placeholder }: { value: string; onChange: (v: string) => void; placeholder?: string }) => { /* ... */ };

export const PostsPaginationBar = ({ page, totalPages, onPageChange, disabled }: { page: number; totalPages: number; onPageChange: (p: number) => void; disabled?: boolean }) => { /* ... */ };

export function PostsList<T>({ items, renderItem, getKey, emptyView, className }: { /* ... */ }) { /* ... */ }

export const PostsListItem = ({ href, title, subtitle, image, actions, isRemoving, onRemove, className }: { /* ... */ }) => { /* ... */ };

export const PostsEmpty = ({ message, onNew }: { message?: string; onNew?: () => void }) => { /* ... */ };
```

Visual: replicar 1:1 o estilo de `apps/web/modules/common/ui/components/entity-components.tsx` (mesmas classes), só trocar nomes para evitar acoplamento.

## Arquivos tocados
- `apps/web/modules/dashboard/posts/ui/posts-entity-components.tsx` — criar.

## Verificação
- [ ] `pnpm --filter web typecheck` passa.
- [ ] Arquivo NÃO importa nada de `apps/web/modules/common/ui/components/entity-components`.
- [ ] Exports usados pelos consumidores das tasks 3.8a–3.8d e 3.9a estão presentes.

## Decisões aplicadas
- Regra de não-reuso (admin-only).
