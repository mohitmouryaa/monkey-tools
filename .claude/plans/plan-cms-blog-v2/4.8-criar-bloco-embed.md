---
id: 4.8
phase: 4
type: criar
depends_on: []
plan_file: plan-cms-blog-v2.md
---

# ✅ 4.8 — Criar bloco renderer: Embed

## Contexto
Renderiza um bloco `embed` salvo pelo `@editorjs/embed` — YouTube, Vimeo,
CodePen, Twitch, etc. P5 = A: aceitar todos os providers padrão do
plugin (que já normaliza o URL no admin para a forma "embed").

## Detalhe técnico
Criar `apps/web/modules/blog/ui/components/blocks/embed-block.tsx`.

Shape do bloco:
```json
{
  "type": "embed",
  "data": {
    "service": "youtube",
    "source": "https://www.youtube.com/watch?v=...",
    "embed": "https://www.youtube.com/embed/...",
    "width": 580,
    "height": 320,
    "caption": "Legenda opcional"
  }
}
```

```tsx
interface EmbedBlockData {
  service?: string;
  source?: string;
  embed?: string;
  width?: number;
  height?: number;
  caption?: string;
}

export const EmbedBlock = ({ data }: { data: EmbedBlockData }) => {
  if (!data.embed) return null;

  return (
    <figure className="my-6">
      <div className="relative aspect-video w-full overflow-hidden rounded-xl bg-muted">
        <iframe
          src={data.embed}
          title={data.caption || data.service || "Embedded content"}
          className="absolute inset-0 h-full w-full border-0"
          loading="lazy"
          referrerPolicy="strict-origin-when-cross-origin"
          sandbox="allow-scripts allow-same-origin allow-popups allow-presentation"
          allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
          allowFullScreen
        />
      </div>
      {data.caption && (
        <figcaption
          className="mt-2 text-sm text-center text-muted-foreground"
          // biome-ignore lint/security/noDangerouslySetInnerHtml: caption do Editor.js
          dangerouslySetInnerHTML={{ __html: data.caption }}
        />
      )}
    </figure>
  );
};
```

Notas:
- `aspect-video` (16:9) cobre 99% dos casos de YouTube/Vimeo.
- `sandbox` aplica restrições de segurança mínimas mas funcionais
  (sem `allow-top-navigation` para evitar redirects).
- Sem fallback custom para serviço desconhecido — se `embed` está
  presente, confiamos no Editor.js.

## Arquivos tocados
- `apps/web/modules/blog/ui/components/blocks/embed-block.tsx` — criar.

## Verificação
- [ ] Sem `data.embed`, retorna `null`.
- [ ] iframe `src` aponta para a URL embed.
- [ ] caption aparece sob o iframe (centralizada).

## Decisões aplicadas
- **P2** (A): server component.
- **P3** (A): arquivo individual.
- **P5** (A): aceitar todos providers padrão; sandbox + allow mínimos
  protegem.
