---
id: 3.1b
phase: 3
type: criar
depends_on: [3.1a]
plan_file: plan-cms-blog-v2.md
---

# ✅ 3.1b — Criar hook `use-posts-params`

## Contexto
Hook client-side dedicado que envelopa `useQueryStates(postsParams)`. Análogo de `use-tools-params.tsx`, dedicado a posts.

## Detalhe técnico
Criar `apps/web/modules/dashboard/hooks/use-posts-params.tsx`:

```ts
import { useQueryStates } from "nuqs";
import { postsParams } from "@/modules/dashboard/posts/posts-params";

export const usePostsParams = () => {
  return useQueryStates(postsParams);
};
```

## Arquivos tocados
- `apps/web/modules/dashboard/hooks/use-posts-params.tsx` — criar.

## Verificação
- [ ] `import { usePostsParams } from "@/modules/dashboard/hooks/use-posts-params"` resolve em client components.
- [ ] `pnpm --filter web typecheck` passa.
